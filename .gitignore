*.out
.idea
.DS_Store

coverage.*

# WASI build artifact (cmd/tascript-wasm) — rebuild with:
# GOOS=wasip1 GOARCH=wasm go build -o tascript.wasm ./cmd/tascript-wasm
tascript.wasm

# Sandbox host binary (cmd/sandbox) — rebuild with `go build ./cmd/sandbox`
/sandbox
